/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { SDKCore } from "../core.js";
import { encodeFormQuery, encodeSimple } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { APIError } from "../models/errors/apierror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import {
  CustomerIdSpecial,
  Direction,
  GetEndpointsRequest,
  GetEndpointsRequest$zodSchema,
  GetEndpointsResponse,
  GetEndpointsResponse$zodSchema,
} from "../models/getendpointsop.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * List all endpoints
 *
 * @remarks
 * This endpoint fetches an endpoints
 */
export function endpointsGetEndpoints(
  client$: SDKCore,
  projectID: string,
  direction?: Direction | undefined,
  next_page_cursor?: string | undefined,
  ownerId?: string | undefined,
  perPage?: number | undefined,
  prev_page_cursor?: string | undefined,
  q?: string | undefined,
  sort?: string | undefined,
  customer_id_special?: CustomerIdSpecial | undefined,
  options?: RequestOptions,
): APIPromise<
  Result<
    GetEndpointsResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >
> {
  return new APIPromise($do(
    client$,
    projectID,
    direction,
    next_page_cursor,
    ownerId,
    perPage,
    prev_page_cursor,
    q,
    sort,
    customer_id_special,
    options,
  ));
}

async function $do(
  client$: SDKCore,
  projectID: string,
  direction?: Direction | undefined,
  next_page_cursor?: string | undefined,
  ownerId?: string | undefined,
  perPage?: number | undefined,
  prev_page_cursor?: string | undefined,
  q?: string | undefined,
  sort?: string | undefined,
  customer_id_special?: CustomerIdSpecial | undefined,
  options?: RequestOptions,
): Promise<
  [
    Result<
      GetEndpointsResponse,
      | APIError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >,
    APICall,
  ]
> {
  const input$: GetEndpointsRequest = {
    projectID: projectID,
    direction: direction,
    next_page_cursor: next_page_cursor,
    ownerId: ownerId,
    perPage: perPage,
    prev_page_cursor: prev_page_cursor,
    q: q,
    sort: sort,
    customer_id_special: customer_id_special,
  };

  const parsed$ = safeParse(
    input$,
    (value$) => GetEndpointsRequest$zodSchema.parse(value$),
    "Input validation failed",
  );
  if (!parsed$.ok) {
    return [parsed$, { status: "invalid" }];
  }
  const payload$ = parsed$.value;
  const body$ = null;

  const pathParams$ = {
    projectID: encodeSimple("projectID", payload$.projectID, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path$ = pathToFunc("/v1/projects/{projectID}/endpoints")(
    pathParams$,
  );
  const query$ = encodeFormQuery({
    "customer_id_special": payload$.customer_id_special,
    "direction": payload$.direction,
    "next_page_cursor": payload$.next_page_cursor,
    "ownerId": payload$.ownerId,
    "perPage": payload$.perPage,
    "prev_page_cursor": payload$.prev_page_cursor,
    "q": payload$.q,
    "sort": payload$.sort,
  });

  const headers$ = new Headers(compactMap({
    Accept: "application/json",
  }));
  const securityInput = await extractSecurity(client$._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client$._options,
    baseURL: options?.serverURL ?? client$._baseURL ?? "",
    operationID: "GetEndpoints",
    oAuth2Scopes: [],
    resolvedSecurity: requestSecurity,
    securitySource: client$._options.security,
    retryConfig: options?.retries
      || client$._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || [
      "429",
      "500",
      "502",
      "503",
      "504",
    ],
  };

  const requestRes = client$._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path: path$,
    headers: headers$,
    query: query$,
    body: body$,
    userAgent: client$._options.userAgent,
    timeoutMs: options?.timeoutMs || client$._options.timeoutMs
      || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req$ = requestRes.value;

  const doResult = await client$._do(req$, {
    context,
    errorCodes: [],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req$ }];
  }
  const response = doResult.value;
  const responseFields$ = {
    HttpMeta: { Response: response, Request: req$ },
  };

  const [result$] = await M.match<
    GetEndpointsResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >(
    M.json(200, GetEndpointsResponse$zodSchema, {
      key: "twoHundredApplicationJsonObject",
    }),
    M.json(400, GetEndpointsResponse$zodSchema, {
      key: "fourHundredApplicationJsonObject",
    }),
    M.json(401, GetEndpointsResponse$zodSchema, {
      key: "fourHundredAndOneApplicationJsonObject",
    }),
    M.json(404, GetEndpointsResponse$zodSchema, {
      key: "fourHundredAndFourApplicationJsonObject",
    }),
  )(response, req$, { extraFields: responseFields$ });

  return [result$, { status: "complete", request: req$, response }];
}
