/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventsCreateEndpointFanoutEvent } from "../../funcs/eventsCreateEndpointFanoutEvent.js";
import { CreateEndpointFanoutEventRequest$zodSchema } from "../../models/createendpointfanouteventop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateEndpointFanoutEventRequest$zodSchema,
};

export const tool$eventsCreateEndpointFanoutEvent: ToolDefinition<typeof args> =
  {
    name: "events-create-endpoint-fanout-event",
    description: `Fan out an event

This endpoint uses the owner_id to fan out an event to multiple endpoints.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await eventsCreateEndpointFanoutEvent(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
