/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { endpointsCreateEndpoint } from "../../funcs/endpointsCreateEndpoint.js";
import { CreateEndpointRequest$zodSchema } from "../../models/createendpointop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateEndpointRequest$zodSchema,
};

export const tool$endpointsCreateEndpoint: ToolDefinition<typeof args> = {
  name: "endpoints-create-endpoint",
  description: `Create an endpoint

This endpoint creates an endpoint`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await endpointsCreateEndpoint(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
