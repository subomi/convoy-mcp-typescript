/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventDeliveriesGetEventDelivery } from "../../funcs/eventDeliveriesGetEventDelivery.js";
import { GetEventDeliveryRequest$zodSchema } from "../../models/geteventdeliveryop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetEventDeliveryRequest$zodSchema,
};

export const tool$eventDeliveriesGetEventDelivery: ToolDefinition<typeof args> =
  {
    name: "event-deliveries-get-event-delivery",
    description: `Retrieve an event delivery

This endpoint fetches an event delivery.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await eventDeliveriesGetEventDelivery(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
