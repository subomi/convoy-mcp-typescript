/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deliveryAttemptsGetDeliveryAttempts } from "../../funcs/deliveryAttemptsGetDeliveryAttempts.js";
import { GetDeliveryAttemptsRequest$zodSchema } from "../../models/getdeliveryattemptsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetDeliveryAttemptsRequest$zodSchema,
};

export const tool$deliveryAttemptsGetDeliveryAttempts: ToolDefinition<
  typeof args
> = {
  name: "delivery-attempts-get-delivery-attempts",
  description: `List delivery attempts

This endpoint fetches an app message's delivery attempts`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deliveryAttemptsGetDeliveryAttempts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
