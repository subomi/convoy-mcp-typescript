/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filtersUpdateFilter } from "../../funcs/filtersUpdateFilter.js";
import { UpdateFilterRequestRequest$zodSchema } from "../../models/updatefilterop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateFilterRequestRequest$zodSchema,
};

export const tool$filtersUpdateFilter: ToolDefinition<typeof args> = {
  name: "filters-update-filter",
  description: `Update a filter

This endpoint updates an existing filter`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await filtersUpdateFilter(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
