/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metaEventsGetMetaEvent } from "../../funcs/metaEventsGetMetaEvent.js";
import { GetMetaEventRequest$zodSchema } from "../../models/getmetaeventop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMetaEventRequest$zodSchema,
};

export const tool$metaEventsGetMetaEvent: ToolDefinition<typeof args> = {
  name: "meta-events-get-meta-event",
  description: `Retrieve a meta event

This endpoint retrieves a meta event`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metaEventsGetMetaEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
