/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filtersCreateFilter } from "../../funcs/filtersCreateFilter.js";
import { CreateFilterRequestRequest$zodSchema } from "../../models/createfilterop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateFilterRequestRequest$zodSchema,
};

export const tool$filtersCreateFilter: ToolDefinition<typeof args> = {
  name: "filters-create-filter",
  description: `Create a new filter

This endpoint creates a new filter for a subscription`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await filtersCreateFilter(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
